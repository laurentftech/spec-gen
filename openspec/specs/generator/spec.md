# Generator Specification

> Generated by spec-gen v1.0.0 on 2026-03-09 23:34
> Source files: src/core/generator/openspec-compat.ts, src/core/generator/openspec-format-generator.ts, src/core/generator/mapping-generator.ts, src/core/generator/adr-generator.ts, src/core/generator/openspec-writer.ts, src/core/generator/spec-pipeline.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Formats structured LLM outputs into clean
OpenSpec-compatible specification files.

## Entities

### ValidationResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of a validation
process, including errors and warnings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| valid | boolean | Indicates whether the validation passed |
| errors | string[] | List of error messages |
| warnings | string[] | List of warning messages |

### SpecGenMetadata

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents metadata for spec generation,
including version, generation time, domains, confidence, and ADR count.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| version | string | Version of the spec generation |
| generatedAt | string | Timestamp when the spec was generated |
| domains | string[] | List of domains covered by the spec |
| confidence | number | Confidence level of the spec generation |
| adrCount | number | Count of Architecture Decision Records |

### DetectedContext

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the detected context of a
project, including tech stack, architecture, domains, and patterns.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| techStack | string | Technology stack used in the project |
| architecture | string | Architecture style of the project |
| domains | string[] | List of domains in the project |
| patterns | string[] | List of design patterns used in the project |

### ContextUpdateOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents options for updating the
context, including whether to preserve user context, append detected info, and the version.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| preserveUserContext | boolean | Whether to preserve user context |
| appendDetectedInfo | boolean | Whether to append detected information |
| version | string | Version of the context update |

### OpenSpecConfigManager

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Manages the OpenSpec configuration file
(config.yaml) for a project.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| configPath | string | Path to the OpenSpec configuration file |
| openspecRoot | string | Root directory for OpenSpec files |

### GeneratedSpec

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a generated specification
file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | File path for the generated spec |
| content | string | Content of the specification file |
| domain | string | Domain the spec belongs to |
| type | string | Type of specification |

### GeneratorOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Options for the generator.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| version | string | Version string for headers |
| style | string | Output style |
| includeConfidence | boolean | Include confidence indicators |
| includeTechnicalNotes | boolean | Include technical notes |
| maxLineWidth | number | Maximum line width for wrapping |

### DomainGroup

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Domain grouping for spec generation.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| description | string | Description of the domain |
| entities | ExtractedEntity[] | Entities in the domain |
| services | ExtractedService[] | Services in the domain |
| endpoints | ExtractedEndpoint[] | Endpoints in the domain |
| files | string[] | Files related to the domain |

### Domain

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a domain within the system,
organizing related functionality and components.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | The name of the domain |
| description | string | A description of what the domain represents |

### SystemCapabilities

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the capabilities that the
system provides.

### DataFlow

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the flow of data through the
system.

### FunctionRef

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a reference to a function in
the codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the function |
| file | string | Relative path to the file containing the function |
| line | number | Line number where the function is defined |
| kind | string | Type or category of the function |
| confidence | enum | Confidence level of the function reference |

### RequirementMapping

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Maps a requirement to its corresponding
functions in the codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requirement | string | Name of the requirement |
| service | string | Name of the service |
| domain | string | Domain of the service |
| specFile | string | Path to the specification file |
| functions | FunctionRef[] | List of function references |

**Relationships:**

- has-many FunctionRef (A requirement mapping can have multiple function references)

### MappingArtifact

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the artifact generated by the
mapping generator.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| generatedAt | string | Timestamp when the artifact was generated |
| mappings | RequirementMapping[] | List of requirement mappings |
| orphanFunctions | FunctionRef[] | List of function references that are not mapped to any requirement |
| stats | object | Statistics about the mapping process |

**Relationships:**

- has-many RequirementMapping (A mapping artifact can have multiple requirement mappings)
- has-many FunctionRef (A mapping artifact can have multiple orphan function references)

### MappingGenerator

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates a requirement→function mapping
artifact from pipeline results and the dependency graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root path of the project |
| openspecPath | string | Path to the OpenSpec directory |
| semanticSearch | SemanticSearchFn | Function for semantic search |

### Mapping

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a mapping between a
requirement and a function.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requirement | string | The name of the requirement |
| service | string | The service and sub-service name |
| domain | string | The domain of the mapping |
| specFile | string | The specification file |
| functions | FunctionRef[] | Array of function references |

### EnrichedADR

Represents an Architecture Decision Record enriched with additional context and metadata.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the ADR |
| title | string | Title of the ADR |
| status | string | Current status of the ADR (e.g., 'proposed', 'accepted', 'superseded') |
| context | string | Contextual information for the decision |
| decision | string | The decision made |
| consequences | string[] | List of consequences of the decision |
| alternatives | string[] | List of alternatives considered |
| relatedLayers | string[] | List of related architecture layers |
| relatedDomains | string[] | List of related domains |

**Relationships:**

- uses ArchitectureSynthesis (ADR uses architecture synthesis data for impact analysis)

### ArchitectureSynthesis

Represents the synthesized architecture data used for impact analysis in ADRs.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| layerMap | Layer[] | Map of architecture layers |

### PipelineResult

Represents the result of the pipeline processing, containing ADRs and architecture synthesis data.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| adrs | EnrichedADR[] | List of enriched ADRs |
| architecture | ArchitectureSynthesis | Architecture synthesis data |

**Relationships:**

- contains EnrichedADR (Pipeline result contains a list of ADRs)
- contains ArchitectureSynthesis (Pipeline result contains architecture synthesis data)

### ADRGeneratorOptions

Options for configuring the ADR generator.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| version | string | Version string for headers |
| includeMermaid | boolean | Flag to include Mermaid architecture diagrams |

### OpenSpecWriter

Handles writing generated specifications to the OpenSpec directory structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| options | OpenSpecWriterOptions | Configuration options for the writer |

### SpecGenerationPipeline

Represents a pipeline for generating specifications from code files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| llm | LLMService | Language model service for generating specifications |
| options | PipelineOptions | Configuration options for the pipeline |

**Relationships:**

- uses LLMService (Pipeline uses LLMService for generating specifications)
- uses PipelineOptions (Pipeline uses PipelineOptions for configuration)

## Requirements

### Requirement: FunctionRefValidation

The system SHALL validate FunctionRef according to these rules:
- confidence must be one of 'llm', 'semantic', or 'heuristic'

#### Scenario: ValidFunctionRefAccepted
- **GIVEN** A valid FunctionRef object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: EnrichedADRValidation

The system SHALL validate EnrichedADR according to these rules:
- ID must be unique
- Status must be one of 'proposed', 'accepted', 'superseded'
- Title must not be empty

#### Scenario: CreateAdr
- **GIVEN** Valid ADR data
- **WHEN** generateSingleADR() is called
- **THEN** ADR markdown file is generated with all sections populated

#### Scenario: GenerateAdrIndex
- **GIVEN** List of ADRs
- **WHEN** generateADRIndex() is called
- **THEN** Index markdown file is generated with a table of all ADRs

### Requirement: SpecGenerationPipelineValidation

The system SHALL validate SpecGenerationPipeline according to these rules:
- Pipeline must have an LLMService and PipelineOptions to be instantiated
- Pipeline stages must be executed in order

#### Scenario: GenerateSpecifications
- **GIVEN** A code file and a configured pipeline
- **WHEN** The pipeline is executed
- **THEN** Specifications are generated for the code file

#### Scenario: ChunkContent
- **GIVEN** A large code file
- **WHEN** The chunkContent method is called
- **THEN** The file content is split into smaller chunks

#### Scenario: GraphPromptForFile
- **GIVEN** A large code file
- **WHEN** The graphPromptFor method is called
- **THEN** A graph-based prompt section is generated for the file

#### Scenario: SaveIntermediateResult
- **GIVEN** An intermediate result
- **WHEN** The saveResult method is called
- **THEN** The intermediate result is saved

### Requirement: Generatespecs

The system SHALL generates all specification files from a pipeline result.

#### Scenario: GenerateSpecsFromPipelineResult
- **GIVEN** A valid PipelineResult object
- **WHEN** generateSpecs is called
- **THEN** An array of GeneratedSpec objects is returned

### Requirement: Groupbydomain

The system SHALL groups entities, services, and endpoints by domain.

#### Scenario: GroupEntities,Services,AndEndpointsByDomain
- **GIVEN** A valid PipelineResult object
- **WHEN** groupByDomain is called
- **THEN** An array of DomainGroup objects is returned

### Requirement: Inferdomain

The system SHALL infers the domain from a name and location.

#### Scenario: InferDomainFromNameAndLocation
- **GIVEN** A name, location, and array of suggested domains
- **WHEN** inferDomain is called
- **THEN** The inferred domain is returned

### Requirement: Generateoverviewspec

The system SHALL generates the overview specification.

#### Scenario: GenerateOverviewSpecification
- **GIVEN** A valid ProjectSurveyResult, array of DomainGroup objects, and ArchitectureSynthesis object
- **WHEN** generateOverviewSpec is called
- **THEN** A GeneratedSpec object is returned

### Requirement: Generatemapping

The system SHALL generates a mapping artifact that associates requirements with functions based on various matching strategies.

#### Scenario: SuccessfulMappingGeneration
- **GIVEN** Valid pipeline results and dependency graph
- **WHEN** generate is called
- **THEN** A mapping artifact is generated with requirements mapped to functions

#### Scenario: SemanticSearchFailure
- **GIVEN** Semantic search function is not available
- **WHEN** generate is called
- **THEN** Mapping artifact is generated without semantic matches

### Requirement: Semanticmatch

The system SHALL performs semantic search to find functions that match a given query.

#### Scenario: SuccessfulSemanticMatch
- **GIVEN** Semantic search function is available and returns results within the maximum distance
- **WHEN** semanticMatch is called
- **THEN** An array of FunctionRefs with semantic confidence is returned

#### Scenario: SemanticSearchFailure
- **GIVEN** Semantic search function is not available or throws an error
- **WHEN** semanticMatch is called
- **THEN** An empty array is returned

### Requirement: Generateadrs

The system SHALL generates all adr specs from pipeline results, including individual adr files and an index file.

#### Scenario: GenerateAdrsWithValidPipelineResult
- **GIVEN** A PipelineResult with valid ADRs and architecture synthesis
- **WHEN** generateADRs is called
- **THEN** An array of GeneratedSpec objects is returned, including individual ADRs and an index file

#### Scenario: GenerateAdrsWithEmptyPipelineResult
- **GIVEN** A PipelineResult with no ADRs
- **WHEN** generateADRs is called
- **THEN** An empty array is returned

### Requirement: Generatesingleadr

The system SHALL generates a single adr markdown file from an enriched adr and architecture synthesis.

#### Scenario: GenerateSingleAdrWithValidInputs
- **GIVEN** Valid EnrichedADR and ArchitectureSynthesis objects
- **WHEN** generateSingleADR is called
- **THEN** A GeneratedSpec object representing the ADR markdown file is returned

### Requirement: Generateadrindex

The system SHALL generates the adr index file with a table of all decisions.

#### Scenario: GenerateAdrIndexWithValidInputs
- **GIVEN** An array of valid EnrichedADR objects
- **WHEN** generateADRIndex is called
- **THEN** A GeneratedSpec object representing the ADR index markdown file is returned

### Requirement: Initialize

The system SHALL initializes the openspec directory structure.

#### Scenario: SuccessfulInitialization
- **GIVEN** Valid root path
- **WHEN** initialize is called
- **THEN** OpenSpec directory structure is created

### Requirement: Getexistingdomains

The system SHALL retrieves a list of existing specification domains.

#### Scenario: DomainsRetrieved
- **GIVEN** Existing OpenSpec directory structure
- **WHEN** getExistingDomains is called
- **THEN** List of existing domains is returned

### Requirement: Writespecs

The system SHALL writes generated specifications to the openspec directory structure.

#### Scenario: SpecsWrittenSuccessfully
- **GIVEN** Valid specifications and OpenSpec directory structure
- **WHEN** writeSpecs is called
- **THEN** Specifications are written to the OpenSpec directory structure and a generation report is returned

### Requirement: Writespec

The system SHALL writes a single specification to the openspec directory structure.

#### Scenario: SpecWrittenSuccessfully
- **GIVEN** Valid specification and OpenSpec directory structure
- **WHEN** writeSpec is called
- **THEN** Specification is written to the OpenSpec directory structure and a write result is returned


## Sub-components

> `OpenSpecWriter` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Detectopenspecversion

> Implements: `detectOpenSpecVersion`

Detects the version of OpenSpec being used in the project

#### Requirement: Detectversion

The system SHALL detect the openspec version by examining the project files and configuration

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Initialize

> Implements: `initialize`

Initializes the OpenSpec directory structure

#### Requirement: Createdirectorystructure

The system SHALL create the necessary directory structure for openspec

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Writespec

> Implements: `writeSpec`

Writes generated specifications to the OpenSpec directory structure

#### Requirement: Checkfileexistence

The system SHALL check if the specification file already exists

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Discoverspecfiles

The system SHALL discover and analyze existing specification files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Mergespecifications

The system SHALL merge the generated specifications with existing ones

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Backupspecification

The system SHALL create a backup of the existing specification file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Validatespecification

The system SHALL validate the complete specification file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Warnuser

The system SHALL warn the user about non-fatal issues or skipped files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Ensuredirectory

The system SHALL ensure the target directory exists and is writable

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Logsuccess

The system SHALL log a success message when the task is completed

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Push

> Implements: `push`

Pushes changes to the OpenSpec directory structure

#### Requirement: Pushchanges

The system SHALL push changes to the openspec directory structure

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Updateconfig

> Implements: `updateConfig`

Updates the OpenSpec configuration with generated specifications

#### Requirement: Normalizedomainname

The system SHALL normalize the domain name in the configuration

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Builddetectedcontext

The system SHALL build the detected context for the configuration

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Updatemetadata

The system SHALL update the configuration with specification generation metadata

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Generatenextsteps

> Implements: `generateNextSteps`

Generates the next steps for the OpenSpec process

#### Requirement: Pushnextsteps

The system SHALL push the generated next steps to the openspec process

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Savereport

> Implements: `saveReport`

Saves the OpenSpec report

#### Requirement: Discoverreportfiles

The system SHALL discover and analyze existing report files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Logsummary

> Implements: `logSummary`

Logs a summary of the OpenSpec process

#### Requirement: Logsuccesssummary

The system SHALL log a success summary of the openspec process

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Fileexists

> Implements: `fileExists`

Checks if a file exists

#### Requirement: Checkfileexistence

The system SHALL check if a file exists

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Discovery

> Implements: `discovery`

Discovers and analyzes files, patterns, and directories

#### Requirement: Discoverfiles

The system SHALL discover and analyze files, patterns, and directories

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Mergespec

> Implements: `mergeSpec`

Merges generated specifications with existing ones

#### Requirement: Extractgeneratedsection

The system SHALL extract the generated section from the specifications

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Logsuccess

The system SHALL log a success message when the task is completed

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Backupfile

> Implements: `backupFile`

Creates a backup of a file

#### Requirement: Discoverbackupfiles

The system SHALL discover and analyze existing backup files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Validatefullspec

> Implements: `validateFullSpec`

Validates a complete specification file

#### Requirement: Validatespecstructure

The system SHALL validate the structure of the specification file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Validaterequirementformat

The system SHALL validate the format of requirements in the specification file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Pushvalidationresult

The system SHALL push the validation result to the openspec process

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Validatescenarioformat

The system SHALL validate the format of scenarios in the specification file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Warning

> Implements: `warning`

Warns the user about non-fatal issues or skipped files

#### Requirement: Warnuser

The system SHALL warn the user about non-fatal issues or skipped files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Ensuredir

> Implements: `ensureDir`

Ensures the target directory exists and is writable

#### Requirement: Ensuredirectory

The system SHALL ensure the target directory exists and is writable

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Success

> Implements: `success`

Logs a success message when a task is completed

#### Requirement: Logsuccess

The system SHALL log a success message when a task is completed

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Run

The system SHALL executes the complete specification generation pipeline, coordinating all stages and handling intermediate results.

#### Scenario: SuccessfulPipelineExecution
- **GIVEN** Valid codebase and configuration
- **WHEN** run is called
- **THEN** All stages complete successfully and results are saved

#### Scenario: StageFailure
- **GIVEN** A stage fails during execution
- **WHEN** run is called
- **THEN** Pipeline stops, error is logged, and partial results are saved

### Requirement: Chunkcontent

The system SHALL splits file content into manageable chunks for processing.

#### Scenario: ContentChunking
- **GIVEN** Large file content
- **WHEN** chunkContent is called
- **THEN** Content is split into chunks with specified overlap

### Requirement: Graphpromptfor

The system SHALL generates a graph-based prompt section for large files.

#### Scenario: GraphPromptGeneration
- **GIVEN** Large file with complex structure
- **WHEN** graphPromptFor is called
- **THEN** Graph-based prompt section is generated

### Requirement: Saveresult

The system SHALL saves intermediate processing results.

#### Scenario: ResultSaving
- **GIVEN** Intermediate processing data
- **WHEN** saveResult is called
- **THEN** Data is saved successfully


## Sub-components

> `SpecGenerationPipeline` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Executestage

> Implements: `executeStage`

Orchestrates the execution of a single stage in the specification generation pipeline

#### Requirement: Stageexecution

The system SHALL execute a single stage in the pipeline, including pre and post processing

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Stageconditioncheck

The system SHALL determine if a stage should be executed based on its dependencies

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Stageprogressupdate

The system SHALL update the progress of the generation pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage1

> Implements: `runStage1`

Generates initial specifications from codebase analysis

#### Requirement: Signaturemapformatting

The system SHALL format signature maps for the codebase analysis

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Stage1execution

The system SHALL execute stage 1 of the specification generation pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Stage1resultmerging

The system SHALL merge the results of stage 1 analysis

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getdefaultsurvey

> Implements: `getDefaultSurvey`

Retrieves the default survey configuration for the specification generation pipeline

#### Requirement: Defaultsurveyretrieval

The system SHALL retrieve the default survey configuration

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Resolvefiles

> Implements: `resolveFiles`

Resolves the files to be processed in the specification generation pipeline

#### Requirement: Fileresolution

The system SHALL resolve the files to be processed, excluding test files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Testfilecheck

The system SHALL determine if a file is a test file

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getschemafiles

> Implements: `getSchemaFiles`

Retrieves the schema files to be used in the specification generation pipeline

#### Requirement: Schemafileretrieval

The system SHALL retrieve the schema files to be used in the pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage2

> Implements: `runStage2`

Generates specifications for the schema files in the codebase

#### Requirement: Contentchunking

The system SHALL chunk the content of the schema files for processing

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Graphpromptgeneration

The system SHALL generate a graph prompt for the schema files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Stage2resultsaving

The system SHALL save the results of stage 2 analysis

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Updategeneration

> Implements: `updateGeneration`

Updates the progress of the specification generation pipeline

#### Requirement: Progressupdate

The system SHALL update the progress of the generation pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Warning

> Implements: `warning`

Handles non-fatal issues, skipped files, and fallback behavior in the specification generation pipeline

#### Requirement: Warningmessage

The system SHALL display a warning message

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Push

> Implements: `push`

Pushes data to a specified destination in the specification generation pipeline

#### Requirement: Datapush

The system SHALL push data to a specified destination

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getservicefiles

> Implements: `getServiceFiles`

Retrieves the service files to be used in the specification generation pipeline

#### Requirement: Servicefileretrieval

The system SHALL retrieve the service files to be used in the pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage3

> Implements: `runStage3`

Generates specifications for the service files in the codebase

#### Requirement: Subspecgeneration

The system SHALL generate sub-specifications for the service files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getapifiles

> Implements: `getApiFiles`

Retrieves the API files to be used in the specification generation pipeline

#### Requirement: Apifileretrieval

The system SHALL retrieve the api files to be used in the pipeline

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage4

> Implements: `runStage4`

Generates specifications for the API files in the codebase

#### Requirement: Stage4resultsaving

The system SHALL save the results of stage 4 analysis

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage5

> Implements: `runStage5`

Generates specifications for the architecture files in the codebase

#### Requirement: Architecturenodeenrichment

The system SHALL enrich the nodes of the architecture

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getdefaultarchitecture

> Implements: `getDefaultArchitecture`

Retrieves the default architecture configuration for the specification generation pipeline

#### Requirement: Defaultarchitectureretrieval

The system SHALL retrieve the default architecture configuration

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Enricharchitecturenodes

> Implements: `enrichArchitectureNodes`

Enriches the nodes of the architecture in the specification generation pipeline

#### Requirement: Architecturenodeenrichment

The system SHALL enrich the nodes of the architecture

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Shouldrunstage

> Implements: `shouldRunStage`

Determines if a stage should be executed in the specification generation pipeline

#### Requirement: Stageexecutioncheck

The system SHALL determine if a stage should be executed based on its dependencies

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Runstage6

> Implements: `runStage6`

Generates specifications for the cost tracking in the codebase

#### Requirement: Costtrackingretrieval

The system SHALL retrieve the cost tracking data

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getcosttracking

> Implements: `getCostTracking`

Retrieves the cost tracking data for the specification generation pipeline

#### Requirement: Costtrackingretrieval

The system SHALL retrieve the cost tracking data

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Saveresult

> Implements: `saveResult`

Saves intermediate results in the specification generation pipeline

#### Requirement: Resultsaving

The system SHALL save intermediate results

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Success

> Implements: `success`

Handles successful completion of tasks in the specification generation pipeline

#### Requirement: Successmessage

The system SHALL display a success message

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Runstage1

The system SHALL orchestrates the project survey process by handling file chunks and merging results.

#### Scenario: SingleChunkProcessing
- **GIVEN** Signatures fit within a single chunk
- **WHEN** runStage1 is called
- **THEN** runStage1WithSection is called once with the entire signature chunk

#### Scenario: MultipleChunkProcessing
- **GIVEN** Signatures exceed a single chunk
- **WHEN** runStage1 is called
- **THEN** runStage1WithSection is called multiple times with each signature chunk

#### Scenario: LegacyFallback
- **GIVEN** No signatures are available
- **WHEN** runStage1 is called
- **THEN** runStage1WithSection is called with the legacy file listing

### Requirement: Runstage1withsection

The system SHALL performs a single llm call to analyze a section of the project structure.

#### Scenario: SuccessfulAnalysis
- **GIVEN** Valid project structure and file listing
- **WHEN** runStage1WithSection is called
- **THEN** A successful StageResult with ProjectSurveyResult is returned

#### Scenario: FailedAnalysis
- **GIVEN** Invalid project structure or file listing
- **WHEN** runStage1WithSection is called
- **THEN** An unsuccessful StageResult with an error message is returned

### Requirement: Mergestage1results

The system SHALL merges multiple stage 1 results into a single result.

#### Scenario: AllSuccessfulResults
- **GIVEN** Multiple successful StageResult<ProjectSurveyResult> objects
- **WHEN** mergeStage1Results is called
- **THEN** A merged StageResult with the highest confidence data and deduplicated file lists is returned

#### Scenario: NoSuccessfulResults
- **GIVEN** No successful StageResult<ProjectSurveyResult> objects
- **WHEN** mergeStage1Results is called
- **THEN** The first StageResult in the array is returned

### Requirement: Testrunstage5

The system SHALL tests the runstage5 function to ensure it correctly processes architecture data and returns a successful stageresult.

#### Scenario: ValidArchitectureData
- **GIVEN** Valid survey, entities, services, endpoints, depGraph, and callGraph data
- **WHEN** runStage5 is called
- **THEN** A successful StageResult is returned with the architecture data

### Requirement: Testdepgraphstatistics

The system SHALL tests that the depgraph statistics are included in the prompt.

#### Scenario: ValidDepgraphData
- **GIVEN** Valid depGraph data
- **WHEN** runStage5 is called
- **THEN** The depGraph statistics are included in the prompt

### Requirement: Testcallgraphdetails

The system SHALL tests that the callgraph details are included in the prompt when provided.

#### Scenario: ValidCallgraphData
- **GIVEN** Valid callGraph data
- **WHEN** runStage5 is called
- **THEN** The callGraph details are included in the prompt

### Requirement: Testsaveintermediate

The system SHALL tests that intermediate results are saved when saveintermediate is true.

#### Scenario: SaveIntermediateResults
- **GIVEN** saveIntermediate is true
- **WHEN** runStage5 is called
- **THEN** Intermediate results are saved

#### Scenario: DoNotSaveIntermediateResults
- **GIVEN** saveIntermediate is false
- **WHEN** runStage5 is called
- **THEN** Intermediate results are not saved

### Requirement: Testllmerrors

The system SHALL tests that llm errors are handled and a failure stageresult is returned.

#### Scenario: LlmError
- **GIVEN** An LLM error occurs
- **WHEN** runStage5 is called
- **THEN** A failure StageResult is returned with the error message

### Requirement: Testuserprompt

The system SHALL tests that the userprompt is constructed correctly with all entity, service, and endpoint information.

#### Scenario: ValidEntity,Service,AndEndpointData
- **GIVEN** Valid entity, service, and endpoint data
- **WHEN** runStage5 is called
- **THEN** The userPrompt is constructed correctly with all entity, service, and endpoint information

### Requirement: Teststageresultshape

The system SHALL tests that the stageresult shape is correct on success.

#### Scenario: SuccessfulStageresult
- **GIVEN** Valid input data
- **WHEN** runStage5 is called
- **THEN** A successful StageResult is returned with the correct shape

### Requirement: Testnodestoclassify

The system SHALL tests that the nodes-to-classify section is included in the prompt when callgraph is provided.

#### Scenario: ValidCallgraphData
- **GIVEN** Valid callGraph data
- **WHEN** runStage5 is called
- **THEN** The nodes-to-classify section is included in the prompt

#### Scenario: NoCallgraphData
- **GIVEN** No callGraph data
- **WHEN** runStage5 is called
- **THEN** The nodes-to-classify section is not included in the prompt

### Requirement: Testdeduplicatenodes

The system SHALL tests that nodes that appear in both hubfunctions and entrypoints are deduplicated.

#### Scenario: OverlappingNodes
- **GIVEN** Nodes that appear in both hubFunctions and entryPoints
- **WHEN** runStage5 is called
- **THEN** The nodes are deduplicated in the prompt

### Requirement: Testnoderoles

The system SHALL tests that noderoles are returned from the llm response.

#### Scenario: ValidNoderolesData
- **GIVEN** Valid nodeRoles data in the LLM response
- **WHEN** runStage5 is called
- **THEN** The nodeRoles are returned in the StageResult

#### Scenario: NoNoderolesData
- **GIVEN** No nodeRoles data in the LLM response
- **WHEN** runStage5 is called
- **THEN** The StageResult does not include nodeRoles

### Requirement: Testsurveydata

The system SHALL tests that survey data is used to prompt correctly.

#### Scenario: ValidSurveyData
- **GIVEN** Valid survey data
- **WHEN** runStage5 is called
- **THEN** The survey data is used to prompt correctly

## Technical Notes

- **Implementation**: `src/core/generator/openspec-compat.ts, src/core/generator/openspec-format-generator.ts, src/core/generator/mapping-generator.ts, src/core/generator/adr-generator.ts, src/core/generator/openspec-writer.ts, src/core/generator/spec-pipeline.ts`
- **Dependencies**: PipelineResult, ProjectSurveyResult, ExtractedEntity, ExtractedService, ExtractedEndpoint, ArchitectureSynthesis, Scenario, DependencyGraphResult, SearchResult, VectorIndex, EmbeddingService, EnrichedADR, GeneratedSpec, OpenSpecWriterOptions, WriteResult, GenerationReport, WriteMode, LLMService, PipelineOptions, SpecGenerationResult, RepoStructure, LLMContext, StageResult, SpecGenerationPipeline, SerializedCallGraph, CompletionRequest
