# System Overview

> Generated by spec-gen v1.0.0 on 2026-03-09 23:34
> Confidence: 90%

## Purpose

OpenSpec is a CLI tool that generates OpenSpec specifications from codebases using LLM analysis. It
provides a structured approach to understanding and documenting software architecture by analyzing
code repositories, detecting architectural patterns, and generating comprehensive specifications.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes call graphs and requirement mappings to identify refactoring priorities and issues in codebase. | [spec.md](../analyzer/spec.md) |
| Api | Defines core data models: ProgressEvent, BaseOptions, InitApiOptions and 9 more. | [spec.md](../api/spec.md) |
| Services | Manages reading, writing, and merging configuration files for spec-gen and OpenSpec. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Formats structured LLM outputs into clean OpenSpec-compatible specification files. | [spec.md](../generator/spec.md) |
| Drift | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Detects and reports discrepancies between code changes and existing specifications. | [spec.md](../drift/spec.md) |
| Verifier | Verifies code files against specifications to ensure they meet requirements and maintain architectural integrity. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: ProjectSurveyResult, ExtractedEntity, ExtractedService and 1 more. | [spec.md](../types/spec.md) |
| Cli | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Manages and displays progress indicators for CLI operations, including spinners, progress bars, and status messages. | [spec.md](../cli/spec.md) |
| Spec | Initialize spec-gen in a project directory. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Validates the structure and content of OpenSpec specifications and configuration files. | [spec.md](../openspec/spec.md) |
| Chat | Provides a set of tools for interacting with the codebase and generating architecture insights. | [spec.md](../chat/spec.md) |
| Stages | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Synthesizes the architecture of a project based on various inputs including project survey, entities, services, endpoints, dependency graph, and call graph. | [spec.md](../stages/spec.md) |
| Mcp-handlers | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides handlers for various codebase analysis operations including full analysis, architecture overview, refactor reports, duplicate detection, signature extraction, mapping retrieval, and spec drift checking. | [spec.md](../mcp-handlers/spec.md) |
| Utilities | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides utility functions to display progress and success messages during various stages of the code analysis and generation process. | [spec.md](../utilities/spec.md) |
| Commands | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Executes the full spec-gen pipeline including initialization, analysis, and generation of specifications. | [spec.md](../commands/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use of layered architecture to separate concerns and improve maintainability
- Integration with multiple LLM providers for flexibility and cost optimization
- Use of vector indexing for efficient semantic search and analysis
- Emphasis on static analysis and code structure for architecture understanding
- Support for multiple programming languages through language detection and pattern matching

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI command → service layer → analyzer layer → repository layer; results are persisted and can be retrieved for further analysis or generation of specifications.

## Technical Notes

- **Architecture Style**: Layered architecture: CLI interface → service layer → analyzer layer → repository layer. This pattern is justified by the need to separate concerns, improve maintainability, and facilitate testing. The layered approach allows for clear separation of business logic, data access, and presentation layers.
- **Security Model**: No explicit authentication or authorization mechanisms are evident in the provided analysis. The tool relies on local file operations and LLM interactions, which may require user configuration for secure access to external services.
- **External Integrations**: OpenAI-compatible APIs, Local LLM providers (Claude Code, Mistral Vibe), Git for version control and diff operations, LanceDB for vector indexing and semantic search
